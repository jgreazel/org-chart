services:
  backend:
    build:
      context: ..
      dockerfile: apps/backend/Dockerfile
    ports:
      - "3001:3001"

  frontend:
    build:
      context: ..
      dockerfile: apps/frontend/Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
